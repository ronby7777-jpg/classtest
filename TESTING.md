# 驗證紀錄

2026-09-07，Windows x64，Electron 42.11.2。

## 已通過

- 5 項 Node.js 單元測試：設定數值校正、負座標螢幕、限制移動區間、小型工作區、異常設定。
- Electron 原生整合測試：開啟桌寵與設定視窗、新增與重新命名隨機動作、自訂協定讀取 GIF、隨機動作 GIF 匯入、調整桌寵大小並驗證原生視窗尺寸、暫停散步時摸頭後返回常態、設定寫入磁碟。
- 使用 Chromium 的合成麥克風完成錄音、保存及播放；沒有使用真實麥克風。
- 設定頁面以 Chrome 擷取完整畫面並檢視版面。

## 實機確認範圍

真實麥克風、實體多螢幕拔插、不同 DPI 與全螢幕應用程式的置頂行為，仍需在使用者電腦確認。GIF 已確認能解碼與載入；動畫沿用 Chromium 原生圖片播放機制。桌面環境的 Electron 畫面擷取遇到 UnknownVizError，因此設定畫面改用瀏覽器擷取，並非桌面合成畫面截圖。

補充：打包後的 app.asar 通過相同整合測試。另在 Chrome 擷取測試 GIF 的多個影格，確認產生兩個不同畫面。


## 番茄鐘手動驗收

- 設定專注／短休息／長休息各 1 分鐘，開始後關閉工作室，確認桌寵倒數繼續。
- 到時確認通知、桌寵提示、專注次數 +1，下一段保持暫停；重開工作室看到一致狀態。
- 暫停、繼續、重設；調整桌寵素材或暫停散步，確認番茄鐘不被重設。
- 完成四次專注，確認進入長休息；休息完成不增加專注次數。
- 正常退出再開啟，確認剩餘時間、設定與次數保留，計時保持暫停。
- 檢查最小視窗 780×640 下的輸入欄、按鈕和捲動；停用 Windows 通知時仍可見桌寵提示。
